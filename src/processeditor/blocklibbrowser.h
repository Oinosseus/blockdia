#ifndef BLOCKLIBBROWSER_H
#define BLOCKLIBBROWSER_H

#include <QWidget>

class BlockLibBrowser : public QWidget
{
    Q_OBJECT
public:
    explicit BlockLibBrowser(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // BLOCKLIBBROWSER_H